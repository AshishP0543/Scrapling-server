# Copy to secret.yaml, fill in real values, then:
#   kubectl apply -f deploy/k8s/secret.yaml
# secret.yaml is gitignored — never commit real keys.
apiVersion: v1
kind: Secret
metadata:
  name: scrapling-secrets
type: Opaque
stringData:
  api-token: "change-me-long-random-string"
  gemini-key: "AIza..."
